import { ScreenPlaceholder } from '@/presentation/components/screen-placeholder';

export default function SearchScreen() {
  return (
    <ScreenPlaceholder
      title="Search"
      note="Keyword search first; the signature semantic search across the whole corpus (always citing his words) comes in v2."
    />
  );
}
