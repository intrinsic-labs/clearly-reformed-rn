import { ScreenPlaceholder } from '@/components/screen-placeholder';

export default function LibraryScreen() {
  return (
    <ScreenPlaceholder
      title="Library"
      note="The unified, magazine-style library of every resource — backed by kdy/v1/all-resources — is the next build slice."
    />
  );
}
