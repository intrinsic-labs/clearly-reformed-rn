import { ScreenPlaceholder } from '@/components/screen-placeholder';

export default function NotebookScreen() {
  return (
    <ScreenPlaceholder
      title="Notebook"
      note="Your cross-media highlights, clips, and notes — local-first in expo-sqlite — land here in a later slice."
    />
  );
}
